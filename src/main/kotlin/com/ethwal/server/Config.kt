package com.ethwal.server

class Config {
    companion object {
        const val isDefaultWalletType = true
        const val isFullNode = true
        const val keystoreDir = "w3j\\keystore"
    }
}